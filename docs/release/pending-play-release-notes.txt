# Play Console 「이번 버전의 새로운 기능」미리 작성 파일.
# 다음 production AAB 빌드 시 record-play-release-notes.sh 가 사용하고,
# 새 versionCode 기록에 성공하면 아래 본문(비주석)은 자동으로 비운다.
# 이 파일이 비어 있으면 git log subject 로 초안을 만든다.
#
# 아래에 문구를 적으세요 (• 불릿 권장):
